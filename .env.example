# Live System One calls only. Unit tests never need this.
# Copy this file and export a real key from the environment. Do not commit it.
#   export TYPESAFE_API_KEY=your-typesafe-api-key
TYPESAFE_API_KEY=
